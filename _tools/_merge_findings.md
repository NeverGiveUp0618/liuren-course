# 六壬神课 → 六壬课程 合并 · 进度与裁决记录

> 2026-08-30。方向：**course 单向吸收内容成为主站，game 原样冻结**
> （练习系统暂不搬，见下「范围」）。

## 范围（2026-08-30 定）

- **搬**：毕法赋详解、六十四课经、象法速查、知识库五屏正文
- **不搬**：整套练习系统（SRS／综合训练／错题本／掌握度／热力图／今日挑战）——
  日后另开发。因它的题目是**从 game 自己的数据动态生成**的
  （`trainK64Pool`／`trainBJPool`／`trainStaticPool`），
  内容一删练习就空掉，所以 **game 的内容留在原处不删**，只是不再维护。
- 连带：game 仓库**保留在线**，五术堂仍是 8 个入口；
  `lr_*` 与 `liuren_course_*` 两套 localStorage **继续并存**，
  `mingli-home/backup.html:103-104` 两条规则都不动。

---

## ⭐ 盘面口径：统一《通解》（2026-08-30 拍板）

`engine.js` 不动。搬过来的数据一律按它复算，与原值不符的在 md 里逐张留〔订正〕注。

### ⚠️ 一处必须记住的教训：抽取口径决定结论

第一轮我用正则扫 `board:{...}` 字面量，得出「306 张盘、20 张分歧」——**是错的**。
`BF_KU` 里有 **79 张盘是 `bfp(r, ju, sc, mt)` 函数生成的**，没有 `yj:` 字面量，
正则一张都没扫到。**改用 node `eval` 整个数组**之后才是全量。

⚠️ 以后再做这类比对，**先确认数据是不是纯字面量**；有辅助函数就必须 eval，
不能靠正则。差别是 20 vs 40，结论完全不同。

### BF_KU 全量结果（260 张盘）

| 类 | 张数 | 处理 |
|---|---|---|
| 月将与局数不合 | 13 | 已按 `off = 13 − 局数` 订正 |
| 三传与《通解》不符 | 40 | 已按 engine.js 复算，留注 |

**`off = (13 − 局数) mod 12`** 已坐实：全库 282 张带局数标注的盘 **270 张符合**，
且 `bfp()` 自己用的就是这条式子——**出错的全是更早手写的 `board:{}` 字面量**。
⚠️ 修的是**月将**不是占时：占时「午」是 bfp 的昼占房规。

### 40 张三传分歧的构成

| 类型 | 张数 | 说明 |
|---|---|---|
| **取下神** | 14 | App 发用＝通解发用**所临的地盘位**。即 `engine.js:1-9` 写明、已拍板的那处两说 |
| **涉害深浅** | 8 | game 按「孟＞仲＞季」判深浅，《通解》按**数克害重数**。假设已 4/4 复现验证 |
| **三传不成链·疑录入错** | 2 | 俱生格、贵害相加格——App 三传不是天盘上连续三位，是坏数据 |
| **待查** | 16 | 以上都不是，**需回原书逐张核**，见下 |

### ⚠️ 尚未了结：16 张「待查」

已按《通解》复算落地，但分歧原因**未查明**，日后要回《图解六壬大全》核：

```
[伏吟] 壬辰 任信丁马格          辰辰辰 ⇒ 亥辰戌
[伏吟] 乙酉 年命丁马格          酉酉酉 ⇒ 辰酉卯
[伏吟] 癸酉 任信丁动中传空亡格   酉酉酉 ⇒ 丑戌未
[伏吟] 甲申 递互作绝神格         申寅巳 ⇒ 寅巳申
[返吟] 甲子 见救不救格          午子午 ⇒ 寅申寅
[返吟] 丙子 结绝格             亥巳亥 ⇒ 午子午
[昴星] 丁丑 互坐丘墓格          子卯午 ⇒ 午戌辰
[昴星] 己丑 移远就近格          午卯子 ⇒ 子辰戌
[重审] 乙丑 四胜煞格            亥辰酉 ⇒ 寅未子
[重审] 甲申 彼此猜忌格          午酉子 ⇒ 申亥寅
[重审] 戊辰 传墓入墓格          戌卯申 ⇒ 寅未子
[重审] 戊辰 见财无财格          辰午申 ⇒ 申戌子
[重审] 癸未 白蚁食尸格          午丑申 ⇒ 卯戌巳
[比用] 甲申 互旺格              申卯戌 ⇒ 戌巳子
[遥克] 戊申 火强格              巳申亥 ⇒ 寅巳申
[知一] 甲子 丁马相加格          卯戌巳 ⇒ 寅酉辰
```

⭐ 4 张伏吟里有 3 张 App 作「三传全同」（辰辰辰／酉酉酉），
**伏吟的中末本应另取（刑／冲）**——像是 game 的伏吟处理偷了懒，值得连它的算法一起看。

⚠️ **`engine.js` 的三传正确性只有 `tests_engine.js` 88 行的逐例白名单兜底，
每门仅 1–3 例（涉害只有 1 例）。** 拿它否定 40 张盘之前，
应先从原书补例进测试——这是这 16 张待查最该先做的事。

---

## 已完成

### ① `content/97-毕法赋详解.md`（100 法 / 356 格 / 260 盘，417 KB）
- 迁移工具 `_tools/_mk97.py`，**跑过一次就够**——md 从此是内容源，别再从 game 重抽
- 盘一律复算，附天地盘＋四课＋三传；**天将不排**（BF_KU 多数条目没记昼夜，宁缺勿造）
- 53 条〔订正〕注（月将 13 ＋ 三传 40），每条都带分歧类型

### ② 接进构建管线
- `build.py`：新增 `build_bifa()` → `data/data-bifa.js`（1.39 MB，**按需加载**）
  ⚠️ `build_lessons` 的排除名单加了 `97-`；**smoke.js 里那份必须同步**（已同步）
- `app.js`：`needBifa()` 按需加载器＋`bflist`／`bifa` 两屏＋首页分母 `#mBifa`
- `index.html`：首页菜单入口＋两个屏
- `sw.js`：`v11-tabbar` → `v12-bifa`
- `smoke.js`：+11 项毕法赋断言

### ③ 自检
```
build.py          ✓ 自检通过
smoke.js          ✓ 173 项全过
_audit_pan.py     ✓ 459 张盘（+260）不自洽 0
_verify_cite.py   ✓ 469 条引文，命中 434，需复核 0
```
⚠️ 97 的出处写作〔大全三·第 N 法〕，**不带页码，`_verify_cite.py` 验不到**——
原书 OCR（`_ref/大全3_毕法赋.txt`）在，日后可以补一个按法号回查的校验。

---

### ④ 六十四课经并进 `content/98-格局详解.md`（25 → 66 格，56 → 90 KB）

⚠️ **又一次抽取口径踩坑**：我先前说「K64 与中册 25 格**零交集**」也是错的——
同一个正则问题，`name:` 没抓到。实际 **23 个格两书重名**。

| | |
|---|---|
| K64（讲义六十四课经） | 64 格 |
| 98（通解中册） | 26 格 |
| **重名** | **23 格** |
| K64 独有 | 41 格 → 新编号 26–66 |
| 98 独有 | 孤寡课、无禄课、绝嗣课 |

- 迁移工具 `_tools/_mk98add.py`（**非幂等**，检测到 MARK 就拒绝重跑）
- **1–25 号一律不动**——`data-ge.js` 的条目 id 就是这个 n
- 23 个重名格：原节末尾追加「讲义（六十四课经）」块，**两书说法并列不替他选**
- 41 个新格：歌诀＋成格条件＋归类；其中 15 格带讲义详解（象曰／术语／断诀／变格）
- ⚠️ 踩坑：原节都以 `---` 收尾，那是**节间分隔线**；直接追加会把它顶到节中间，
  讲义块和下一节粘死。已改成摘掉尾巴、插完补回
- ⚠️ 新节必须给 `**一句话**` 行——`build_ge` 靠它填列表摘要，缺了列表上是空白

### ⑤ 两个自检脚本的识别范围跟着放宽

- `smoke.js`「每格都带中册出处」→ 改成「通解中册**或讲义**」，
  否则 41 个新格全被判成缺出处；另加一条「23 格并列两说」防后续编辑抹掉
- `_audit_content.py` 的引文出处识别只认 `〔通解`，
  已加 `〔讲义` 与 `〔大全`（曾一次误报 40+ 条）。⭐ `--selftest` 复跑仍全报得出来

### ⑥ 全量自检（两批内容并入后）
```
build.py          ✓ 课25／毕法100法356格／格局66格／课例189
smoke.js          ✓ 174 项全过
_audit_pan.py     ✓ 459 张盘不自洽 0
_verify_cite.py   ✓ 469 条引文，需复核 0
_audit_content.py ✓ 全库仅 6 条提示，新内容占 2 条，**均为误报**：
                    · 97:3875「玄武为癸亥极阴之神」——原书在讲玄武五行性，非天将配干支
                    · 98:1971「子巳合为死字」——铸印课特有字象，非六合
                    两条都忠实原文，**别照单改**
```
`sw.js` → `v13-k64`

### ⑦ `content/96-象法速查.md`（265 条 / 74.6 KB）

| 库 | 条数 | 组 | 出处 |
|---|---|---|---|
| 取象法则 | 106 | 29 | 《图解六壬大全》第 1、2 部 |
| 分类类神 | 113 | 23 | 同上 |
| 百章歌断诀 | 46 | 8 | 《通解》p110-111 · 李九万六壬百章歌 |

- 迁移工具 `_tools/_mk96.py`；`build_xf()` → `data-xf.js`（154 KB，按需加载）
- **自成一屏，刻意不并进「速查」屏**——那屏按第几课分组、每条带
  「到第 N 课看讲解」按钮，象法条目不属于任何一课，塞进去要在 renderXf 里到处特判
- ⚠️ game 里同屏的 `FX_CATS` 是**练习入口**（`fn:startZM` 这类函数引用），
  不是内容，**没有迁**；smoke 里加了一条断言盯着它别混进来

### ⑧ 两个易复发的坑，已改成规则

**① 附篇排除名单** —— `build.py` 与 `smoke.js` 各写死一份 `(00|98|99)` 清单，
**加一次附篇就红一次**（97 红过一次、96 又红一次）。
已双双改成规则：**00 是总目录，编号 ≥ 90 的都是附篇**。以后加 95- 之类两边都不用动。

**② smoke 里 eval 数据文件** —— `DATA_XF` 是**对象**，
`{...}` 在语句位置会被当成代码块而不是对象字面量，必须套一层括号。
（`DATA_BIFA` 是数组所以那边不加也没事，容易踩空。）

### ⑨ 全量自检（三批内容并入后）
```
build.py          ✓ 课25／象法265／毕法100法356格／格局66格／课例189
smoke.js          ✓ 183 项全过
_audit_pan.py     ✓ 459 张盘不自洽 0
_verify_cite.py   ✓ 需复核 0
_audit_content.py ✓ 全库 6 条提示，96 新增 0 条
```
`sw.js` → `v14-xiangfa`

---

## ⭐ 知识库重叠分析（2026-08-30，动手前先做的）

⚠️ **先更正两处我先前说错的**：
1. 「知识库五屏正文约 5 万字」——`kh-intro`/`kh-start`/… 这五个是**导航面板**不是正文，
   真正的内容在它们 16 个入口指向的屏里。其中 5 个是练习（闪卡／记忆宫／出题），留在 game。
2. `BYS_DATA` 不是「八煞九宝」，是「**课式八要素**」（太岁·月建·日辰·占时·月将·空亡·本命·行年）。
   变量名 BYS ＝ 八要素。八煞九宝在第 11 课，是另一回事。

⚠️ **字面重叠率不能当重复度用**：6-gram 重叠全在 0–19%，连 LX_ZHI 对 08 课都是 **0.0%**——
各写各的话，字面本来就不重合。**必须看主题层**。

### 静态 HTML 屏（合计 42,640 字）

| 屏 | 字数 | 课程对应 | 判断 |
|---|---|---|---|
| `pbdl` 六壬运作原理·白话入门 | 4,280 | 第 1 课 | 重复 |
| `p-xf` 壬学心法 | 2,689 | 00 总目录 | **部分独有**：学习次序与取象态度 |
| `pqk` 起课规则与查表 | 1,278 | 第 2–7 课＋速查 137 表 | 重复 |
| `p9z` 九宗门完整体系 | 8,902 | 第 5、6、18 课 | 大部分重复；**「课名由来」独有** |
| `pzfl` 算法逻辑梳理·进阶 | 4,294 | 第 6 课流水线 | 重复 |
| `p5` 天将与支神 | 31,753 | 跨 1,2,3,4,5,6,7,10,15,16 课 | 混合；**历史章 2,890 字课程完全没有** |
| `pgz` 干支关系 | 1,854 | 第 10、11 课 | 重复 |
| `pyus` 旺相与断法 | 1,002 | 第 7、12 课 | 重复 |

### JS 动态数据（HTML 里只有二三十字的空壳）

| 数据 | 字数 | 课程对应 | 判断 |
|---|---|---|---|
| `LX_ZHI` 支神类象·白话（12 条：形象／天象／人物／身体／物品／姓氏） | 3,610 | 第 8 课 | **互补** |
| `LX_TJ` 天将类象·白话（12 条＋临十二支逐条） | 5,806 | 第 9 课 | **互补** |
| `BYS_DATA` 课式八要素（8 条，各带「应用·讲义七条」） | 6,352 | 散在 2／4／7／20 课 | **独有**：课程无此章 |

⭐ **「互补」不是我判的，是课程自己写的**：
- 08 课：「完整清单请查『六壬神课』App 的**象法速查**；这里只讲**怎么用**和每支的骨架。」
- 09 课：「其余各将……**用时去查，不必背**。（App 的象法速查已收录。）」

课程当初就**故意把查表清单外包给 game**。game 一冻结，这些指路就悬空了。

### ⚠️ 13 处指向 game 的交叉指路，其中一批已过时

`grep "六壬神课\|App 的" content/*.md` 共 13 处。**第 25 课那张「工具表」5 项里已有 3 项过时**——
象法速查、六十四课经、毕法赋都已搬进本站，还指着 App。

**这批指路必须随迁移一起更新**，否则读者被指去一个不再维护的站。

### 结论：该搬约 2.1 万字，不搬约 4.2 万字

**搬**
1. `LX_ZHI` ＋ `LX_TJ`（9,416 字／24 条）→ 并进 `96-象法速查` 作第 4、5 库
   ——正是 08／09 课指向的那份清单
2. `BYS_DATA` 课式八要素（6,352 字）→ 新篇或并进第 7 课
3. `p5` 历史章（2,890 字）→ 新 `95-六壬源流`（三式对比／考古实证／唐六典／历朝脉络／天文三才）
4. `p9z` 课名由来 → 补进第 5、6、18 课
5. `p-xf` 壬学心法（2,689 字）→ 并进 00 总目录

**不搬**：`pbdl`／`pqk`／`pzfl`／`pgz`／`pyus`／`p5` 其余 ≈ 4.2 万字，均与课文重复

**必做**：更新那 13 处指路

---

## 知识库迁移执行结果（2026-08-30）

### ⚠️ 又一处改判：`p-xf`「壬学心法」不搬

我原先按 hub 的一句话描述判成「学习次序与取象态度」，建议并进 00 总目录。
**实际打开一看不是**——内容是「大六壬排盘推演与底层逻辑全解」：
- 第一部分：庚午日戌时实例的**七步起盘全过程** → 与第 1–7 课全面重复
- 第二部分：用**数据建模／状态机**比喻讲四课三传（对象模型、执行流水线、克＝异常触发器）
  → 「四象架构」那套（我的现状／我的底牌／对方现状／对方底牌）**第 4 课已有**
  （`04:107` 表格：日阴＝我的隐情、支阴＝对方的隐情），只是措辞不同

只剩「编程比喻」这一层是新的，且与教材「古籍引用＋白话」的语气不搭。**判定不搬。**

⭐ 教训：**hub 上的一句话描述不能当内容摘要用**，必须打开屏本身看。

### 已完成

| 项 | 落点 | 体量 |
|---|---|---|
| `LX_ZHI` ＋ `LX_TJ` 两套白话类象 | `96-象法速查` 第 4、5 库 | 24 条／9.4 千字 |
| `p5` 源流段 | 新 `94-六壬源流.md` | 5.9 KB |
| `BYS_DATA` 课式八要素 | 新 `95-课式八要素.md` | 8 项／14.1 KB |
| `p9z` 十条课名由来 | 补进第 5 课（4 条）、第 6 课（6 条） | 05: 205→237 行，06: 260→304 行 |
| 过时指路 | 08／09／19／00×2／01／25 共 7 处改写 | — |

⚠️ **课名由来只取「释名＋盘面对应」，不取后面的「判断／取法」**——
那些与 05／06 课重复，而且**涉害那条写的是「孟＞仲＞季」深浅法**，
与已拍板的《通解》口径相反，搬进来就是在课文里埋一条自相矛盾。已验证未混入。

### 顺带修的三处

1. **wiki 链接对附篇无效** —— `bindDoc` 只按 `M.list` 路由到课文，
   `[[96-象法速查]]` 这类点了没反应。已加 `EXTRA_ROUTE`（94→附篇/源流、95→附篇/八要素、
   96→象法、97→毕法、98→格局、99→课例）。
2. **`94` 标题跳级** —— h1 直接跳 h3，`_audit_content` 报了。已改成 h2。
3. **`_audit_content` 把「盘面对应」「一句话」当引文** —— 那是我写的提示块，
   里面的「」是强调不是引文。已加进 hint 白名单（与 ⭐/💡/⚠️ 同类）。
   ⭐ `--selftest` 复跑仍报得出真的无出处引文。

### 全量自检（四批内容并入后）
```
build.py          ✓ 课25／附篇2／象法289／毕法100法356格／格局66格／课例189
smoke.js          ✓ 193 项全过
_audit_pan.py     ✓ 459 张盘不自洽 0
_verify_cite.py   ✓ 469 条引文，需复核 0
_audit_content.py ✓ 6 条提示，与迁移前持平（均为已判定的误报）
```
`sw.js` → `v15-full`

---

## 待办
- [ ] 上面 16 张待查
- [ ] 给 `tests_engine.js` 补涉害／伏吟例盘
- [ ] game 侧：涉害正文（1035／1412／1910 行）与算法（2486–2490 行）自相矛盾，需修
